export function sigma(start, end) {
    /*
    let result = 0;
    for (var k = start; k <= end; k++) {
      result += k
    }
    console.log(result);
    return result;
  }
  */
  return "Hello from sigma"
}